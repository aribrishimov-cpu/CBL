import java.awt.*;
import javax.swing.*;

public class OrdersPanel extends JPanel {
    public OrdersPanel() {
        setPreferredSize(new Dimension(300, 640)); // same height as map panel
        setBackground(Color.GRAY);
    }
}